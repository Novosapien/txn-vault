---
status: Living
description: "Central register of all 67 open questions: type, area, status and evolving answers per row, and the source table for the branded HTML gap register"
---

# TXN — Open Questions

> **Index:** [[index]] · **Vision:** [[vision]] · **Components:** [[components]]

Central register of unresolved questions across the vault. Each links to the doc/section where the answer belongs. Docs carry a short inline marker — `[⚠ open — see [[open-questions]] #N]` — instead of burying the full question at the bottom of every file.

This is the **single source of truth** for open questions. The branded **[gap register](drafts/txn-gap-register.html)** (HTML, for partners) is generated *from* this table — regenerate it when this changes.

**Status:** `Open` (needs an answer) · `Answered` (resolved — keep the row, note where it landed) · `Parked` (deferred).
**Type:** `Build` (build-blocking — ours to build, blocks the dependent piece) · `Client` (TXN product / commercial / risk decision) · `Partner` (needed from DT / Stackworkz / Super Ultra / a provider) · `Design` (our design work — land &amp; iterate).

## Register

| # | Question | Type | Area | Answer goes in | Raised in | Status |
|---|----------|------|------|----------------|-----------|--------|
| 1 | Product configuration — is it **~200 fields** or **50–60 properties**? (Two different figures from two calls.) **Answered (11-06):** don't fixate on a number — config is large and *grows*, and **nothing is mandatory** (the platform is deliberately flexible). Resolve by **configuring at feature level** (turn a feature on → fill only its fields, e.g. address verification's ~7) with **AI recommendations by region/vertical** ("UK needs these 4"; "95% of BNPL clients do this"). Client stays liable. | Client | Co-pilot / Access Layer | [[vision]], [[co-pilot]], [[agent-access-layer]] | 13-05 / 01-06 | Answered |
| 2 | MCP-server **permission model** — do we need both **program-defined** and **user-defined** permissions, with capabilities switchable on/off at each level? How do API keys/instances map to this? **Update (18-06):** confirmed this stays a **test-driven open design decision** — the high-risk-action policy (what the agent may do, for which users) will be settled through testing/simulation, not up front. | Design | Access Layer | [[mcp-server]], [[permission-scoping]] | 01-06 | Open |
| 3 | **Site-wide LLM ownership** — 29 May named **Teraflow** for the site-wide/agentic AI. **Resolved:** *Teraflow is Novosapien's former brand (since rebranded to Novosapien)* — the same party. The site-wide / agentic LLM is **Novosapien's**. | Client | Developer Support | [[developer-support]] | 29-05 | Answered |
| 4 | **System-defined alert corpus** — which alerts is TXN obliged to raise? (Specifics not defined; the focus so far is the *handling process*, not the catalogue.) **Working answer (11-06):** still being scoped (Mike pushing on it); partly blocked on getting into the DT back end to see what's logged. Known so far: **core transaction declines** is the priority + infra-level thresholds; signals will sit in central logging (Datadog-type) + the data lake. | Client | Inbox & Alerts | [[alert-detection]], [[agent-inbox-alerts]] | 02-06 | Open |
| 5 | **Idempotency** on mutating MCP calls — assumed, not discussed. Confirm the requirement. | Design | Access Layer | [[mcp-server]] | (assumption) | Open |
| 6 | **Audit immutability / append-only + retention** — assumed immutable; retention window and storage location undecided. **Working answer (11-06):** immutable confirmed; storage → the **DT data lake** (already holds an intensive core/DT audit history; the AI can push into it). Hot-vs-cold split + final data architecture to confirm with DT; note the **7-year PCI retention** requirement (where it lives still being worked). | Build | Access Layer | [[audit-attribution]] | 01-06 | Open |
| 7 | **Notification delivery** — fallback/retry on failure and a default channel were assumed, not discussed. Confirm. | Design | Inbox & Alerts | [[notification-routing]] | (assumption) | Open |
| 8 | **MCP-server ownership split** — docs/dev-portal MCP (Stackworkz wants it) vs card-acquiring-API MCP (owner unresolved, maybe DT). Who builds which; avoid duplicates. **Answered (18-06):** split confirmed — **docs / dev-portal MCP = Stackworkz; card-acquiring-API MCP = DT.** DT will own and manage **all** of it post-handover; these two splits hold for the current development. | Partner | Access Layer | [[agent-access-layer]] | 29-05 | Answered |
| 9 | **AI-permission config** — TXN must build the org/user permission mirror for non-Console access (no API today). When, and owned by whom? **Answer (11-06):** approach agreed — the **org holds the permission set (≈super-user); users get subsets**. The AI bridges two cases: *system-to-system* (client app → our AI, like system-to-API) and *user-to-system* (e.g. Claude). Gate tools per **user ID** (look up their permissions → turn tools on/off); for the Claude/MCP path use **OAuth** to bind the *user* (not the org). A **separate AI/MCP key** is likely (AI perms may be more granular than the API; Console already has sections for API/AI/MCP keys). Key format (JWT vs key) TBD with DT; long-lived, with suspend/rotate/refresh on leak. **Update (18-06):** the key-format question is now live — a DT document proposes **JWT** over long-lived keys (see #31). **Update (24-06):** **JWT confirmed** — reuse **Console authentication** and append the **user ID to every tool call / data-access request**, so every agent action is authenticated and scoped to the user's Console permissions. Auth *mechanism* settled; the org/user permission-mirror build remains. | Build | Access Layer | [[agent-access-layer]] | 01-06 | Open |
| 10 | **Product webhook** — will DT add it, and on what timeline? (Underpins change-impact alerts.) **Update (18-06):** webhooks sit **late in DT's delivery order** (after Visa certification — they're not needed for cert). Novosapien has called out the specific webhooks it needs and sent the wider list; DT hasn't scoped them or written user stories yet, so adoption should be straightforward but the **timeline is still TBD** (Mike to share DT's delivery schedule; a request doc/CR may follow). | Partner | Access Layer / Inbox & Alerts | [[agent-access-layer]], [[alert-detection]] | 01-06 | Open |
| 11 | **Core API stability + versioning policy** — when is it stable enough to build tools against; how are breaking changes managed? **Update (18-06):** touched only briefly ("we've had a chat about that already"); no new resolution. | Partner | Access Layer | [[agent-access-layer]] | 01-06 | Open |
| 12 | **Data-lake schema** — when does DT share it? **Working answer (11-06):** DT is actively building it (draft architecture diagram incoming; still early/changeable). Structure: **raw → curated → consumption** layers (rollup tables for dashboards); **tabular SQL + JSON/vectors** for AI (DT recommends both; **1536-dim** vectors, OpenAI standard); **row-level security** (client A can't see client B; bin-sponsor sees their clients); per-program identifier; settlement/clearing data stored for later reconciliation; ~1M+ txns/month per large client; ISO→JSON normalisation. EU residency + 7-yr PCI still being worked. **Update (18-06):** DT's presentation is **still owed** (Dorte chasing). DT's own data is static / central-API-oriented; Novosapien will use it far more aggressively and granularly (Console). Propose a **joint DT + Novosapien session**; Mike is still securing direct DB access to learn the structure. | Partner | Access Layer | [[agent-access-layer]] | 01-06 | Open |
| 13 | **Alerting-system build ownership** — DT's role was narrowed (post-AI discussion) to a context-less "post an alert" API; nobody is currently scoped to build the alerting/detection system. Who owns it? **Update (18-06):** direction — a **standard observability platform triggers the alert**, then something acts on it; this now sits inside the DT logging/alerting/monitoring architecture discussion. Target: a **central alert store** that the Console pulls from and surfaces per program (graded/flagged so users aren't drowned). Whether **DT pushes** alerts or **Novosapien holds** them centrally, and who owns the inbox, is still being scoped with DT. | Partner | Inbox & Alerts | [[agent-inbox-alerts]], [[alert-detection]] | 02-06 | Open |
| 14 | **Scheduled-report failure handling** — are missed/failed runs retried, and is the user notified of a delay vs a silent drop? Assumed in [[scheduled-reporting]], not discussed. | Design | Inbox & Alerts | [[scheduled-reporting]] | (assumption) | Open |
| 15 | **Alert priority field + multi-surface fan-out** — is there a `priority` field on the delivery payload, and do high-priority items fan out to multiple surfaces at once? Asserted in [[notification-routing]], not discussed. | Design | Inbox & Alerts | [[notification-routing]] | (assumption) | Open |
| 16 | **AI-layer PII / data-residency / redaction** — may cardholder PII appear in chat/audit logs, and what residency + redaction policy applies? Asserted in [[audit-attribution]] beyond the grounded retention point (#6). **Answered (11-06):** policy — **limit PII** (don't send what isn't needed); **redact PII from audit/logs** (keep logs clear); **EU data stays in Europe** (residency concern is data *at rest*; in-transit is fine; get a storage acknowledgement from any third party). Provider APIs (Gemini/Vertex, OpenAI/Azure, Claude/AWS) are secure now; confirm standards given it's a financial product. | Client | Access Layer | [[audit-attribution]] | (assumption) | Answered |
| 17 | **AI-analysis consistency boundaries** — must the AI avoid analysing before a write settles (read-after-write guards)? Stated in [[ai-analysis-impact]] but flagged in [[vision]] §8 as a Novosapien inference. | Design | Inbox & Alerts | [[ai-analysis-impact]] | (assumption) | Open |
| 18 | **User-defined optimization metric** — should the user set an objective the agent optimises toward, with the agent able to suggest additional objectives? Floated 13-05; not yet scoped. **Answered (11-06):** not a separate feature — the **objective is the user's request per interaction**; for long-horizon tasks the agent builds a **checklist/task-list** (planned anyway), and modern models retain the message-1 goal. | Client | Full Agentic Experience | [[full-agentic-experience]] | 13-05 | Answered |
| 19 | **CRM ↔ Console data split** — exactly which client data items live only in the CRM vs are needed in the Console, and how they sync (Dorte raised). **Answered (11-06):** **CRM = sales/business + onboarding data** (projections etc.); **Console DB = operational/technical**. Handoff at **contract signing** — future state: the CRM triggers the client build in the Console; the technical build captures only **name + email** (for grouping/permissions), not a full sync; UAT + production auto-built (or triggered for Mike) at that point. | Client | Architecture / Internal Ops | [[architecture]], [[integrations]] | 10-06 | Answered |
| 20 | **AI-ready downloadable doc file** — the Super-Ultra-designed `LLMS.txt`-style file (less than the MCP: "here's TXN, here are the endpoints"); who builds it (Stackworkz from portal data vs AI-generated) and on what release cadence. **Update (18-06):** the file will **live in Umbraco** (Stackworkz host it; format flexible — text is fine). They just need to know where it's hosted and how to pull it. Plan: a **static file at launch**, **autogenerated later**; Novosapien may **seed a static version** from the docs; Mike to confirm a placeholder with Stackworkz. May be hidden in the initial July portal and return in a phase two. | Partner | Developer Support | [[developer-support]] | 09-06 | Open |
| 21 | **Sign-up / login design** — not scoped in the portal MVP (docs are public). Needed to gate the MCP/sandbox and capture leads. Depends on #31. **Working answer (11-06):** **phase two** of the dev portal (portal ready end-July; next phase may line up). Even without a full login, **capture email + company** to access the AI; this is a **separate, lighter MCP** (docs + sandbox) from the paid in-use one; **track every request / endpoint / doc** the lead hits to feed the sales process; later migrate Gmail → work-email accounts. Company as a (near-)mandatory field; don't gate out early-stage users. **Update (18-06):** confirmed **no login/sign-up at the July portal launch** (so #29 is not urgent yet). | Build | Developer Support | [[access-gating]], [[developer-support]] | 09-06 | Open |
| 22 | **Persistence store** — where do saved rendered views + recurring-task definitions live (state model for revisitable dashboards)? **Answered (11-06):** store in the **same Stackworkz/Console database** (single source of truth + one clean IO — better if also rendering in the Console; avoids two DBs). Recurring tasks = a scheduled job (minor design). Caveat: don't overload / "dirty" their DB with all the agentic caching — watch volume. | Build | Full Agentic Experience | [[session-persistence]] | 05-06 | Answered |
| 23 | **External name for "Co-pilot"** — "Co-pilot" is a Microsoft trademark (Dorte). Internally it needn't be named; decide the external/market name before launch collateral. **Discussed (11-06):** names floated (Nova, Dot, Jarvis, Terry…) but **explicitly still undecided** — to be named later. **Update (18-06):** more names floated (Nexa, Access, Tony); Dorte favours **Nexa**. Still low priority / not decided — park until launch collateral. | Client | Co-pilot | [[co-pilot]] | 04-06 | Open |
| 24 | **Per-level cost / token model** — the free surfaces (portal, MCP, sandbox) need a cost-per-1000-calls understanding to set the access gates (Ian). **Update (18-06):** a **token-costing calculator** now exists (Brett → Ian): **~$1.15 per user** blended (internal + external, every use case found), assuming a held model with **~30%/yr** model-cost decline; downloadable HTML tool with scenarios, cost multipliers, exchange rates, contingency/overheads. **To add:** a model-price-**increase** scenario (Dorte). Per-level gate costs still need Ian's sign-off → stays Open. **Update (24-06):** model-optimisation lever confirmed — larger models (e.g. Opus 4.8) optimise the prompts/pipelines of smaller **production** models (big models for testing, small for prod). | Client | Developer Support / Alerts | [[developer-support]], [[agent-inbox-alerts]] | 09-06 / 02-06 | Open |
| 25 | **Reconciliation's home** — auth/clearing settlement reconciliation fits neither the client lanes nor fraud cleanly. Own payment-ops component, or stays in Internal Ops? **Answered (18-06):** it becomes its **own component**, **dual-use** — Novosapien uses it internally to verify things work, but **clients will use it heavily** too, so it's bigger than internal ops. Kept cross-referenced from Internal Ops. **Needs a short (~30-min) capture session**; DT data format/structure still unknown (waiting on DT). Flagged in [[components]] and [[internal-ops-agents]] (own **Reconciliation** component pending a capture session). | Client | Internal Ops / Reconciliation | [[internal-ops-agents]] | 06-03 / journeys | Answered |
| 26 | **AI risk-tolerance boundaries** — what AI actions are *never* allowed even with approval? What blast radius forces a human override regardless of trust level? **Update (18-06):** the **API layer already covers most** — no deletes on cards/cardholders; only public-facing APIs are exposed (no whole-program / whole-TXN deactivation); internal-only actions are never surfaced. So the AI can only do what a client could already do via their own script/AI. Add a **confirmation step on large-scale changes** ("you're about to change 100 products — are you sure?"). Define the remainder **as each use case is built** (detailing them all up front is overkill). **Update (03-08):** first concrete steer from Michael after seeing suspension gated in the demo — **start tight, loosen later**. Two ranking signals: **irreversibility** (suspend is recoverable and the API/Console are relaxed about it; **terminate is not**, and *"pretty much everything has a termination state or a transition endpoint"*, so terminal transitions carry the governance) and **volume** (the agent *"could be doing 100, 200 at once"*, so bulk needs its own gate even for reversible actions). **Lifecycle transitions** are the bulk of the conditional logic (active → suspended → active or terminated; suspended → terminated is final). This **sharpens the June single-card-terminate rule** in [[approval-queue-integration]] (privileged, no second approver) rather than settling it — the tension between "blast radius of one card" and "cannot be undone" is now explicit and still open. Michael to come back with concrete flow proposals. **Update (13-08):** approval **fatigue** is now the observed problem in the working build (*"approval after approval"*). Direction from Michael: **group approvals by common action** rather than one per tool call. Creating a cardholder and their card is one routine action and carries a **single approval**; the **PIN stays separate** because it is set separately; **sensitive payment actions stay separate**. He is documenting the groupings and will return with them. Approval cards also become **collapsible** (small card with approve/reject, expandable for detail), which raises its own question: how much detail does a user need in order to responsibly validate a change. **Update (18-08):** still unresolved and now the main open item on [[approval-queue-integration]]. The approach being explored is having the **agent queue the approvals** rather than serving them one at a time, which turns the question into how to render a queue without losing what each item commits the operator to. Michael's groupings are still owed. **Update (20-08): both halves now moving.** Michael committed to marking **where an approval is expected and where it is not** on each workflow, plus validating the endpoints each calls. And a design is proposed for the stacking: the agent **writes all the tool calls up front**, renders and caches them together, reworking from the point of any rejection, instead of serving cards one at a time. | Client | Cross-cutting | [[vision]], [[agent-access-layer]] | 13-05 §8 | Open |
| 27 | **Cross-program benchmarking consent** — with few clients per use case, "anonymised" comparisons can still identify a client. Consent model + identification thresholds (later phase). **Update (18-06):** direction — **summary data only** ("in my industry, who has this setting on"); **never** PII (cardholder names/addresses), **never** the client name, **never** PAN/CVV (secure-card data is on a separate role-gated endpoint anyway). Map **field-by-field from the API spec** what may/may not be shared; DT does not mask PII in card payloads, so Novosapien must ensure it never sends it back. A test: an **agent-interrogates-agent** check to try to de-anonymise. | Client | Inbox & Alerts | [[agent-inbox-alerts]] | 02-06 | Open |
| 28 | **In-product pricing transparency** — is pricing surfaced inside the Console (so the co-pilot can reference it) or held externally? **Answered (18-06):** pricing is **not surfaced** to the AI or the website — it's bespoke per client (a single platform-access fee; what they use is up to them, covers Console + API). Future **billing/usage** display is possible (maybe tied to invoicing) but not pricing. Pricing/specific-cost questions **route to the assigned account manager / CS** (details pulled from the CRM); routes for "don't let the AI answer this" can be mapped explicitly. | Client | Co-pilot | [[co-pilot]] | 13-05 §8 | Answered |
| 29 | **Corporate-email gating policy** — preferred to avoid junk signups (the Marqeta lesson), but don't block genuine startups without a corporate email. **Update (18-06):** **personal / junk email is acceptable** — many early fintechs won't have a company name yet. The priority is that an account can **transition** to a proper client later without losing context (dev portal → console → TXN). No sign-up at the July launch (#21), so **not urgent**; revisit when sign-up is designed. | Client | Developer Support | [[access-gating]] | 09-06 | Open |
| 30 | **Voice support (Tier 2)** — a voice support agent (~18–30p/min) vs text-first. Validate with a user group post-launch. **Update (18-06):** **text-first at launch** confirmed; introduce voice when **(a) demanded** (sell it as a paid add-on) or **(b) ticket volume** justifies it. Sits in a **layered support model** (docs → text agent → voice → human), with an **agent workbench** that can be dispatched to collect missing info from a user rather than email-and-wait. | Client | Developer Support | [[support-triage]] | 09-06 | Parked |
| 31 | **Public sandbox API-key model** — does the sandbox need a per-user API key? This *changes whether/when sign-up (#21) is required*. **Update (18-06):** **escalated to Stackworkz as top-priority** (one of three blockers: the AI LLM, the public sandbox API key, missing YAML bits). Sandbox **not built yet**. Auth model under review — a DT document now proposes **JWT tokens** over long-lived API keys: security upside, but a **functional shift for clients** (refresh flow vs one endpoint). Keys rotate ~6-monthly today and are client-rotatable in the Console. Decision pending (ties to #9). **Update (24-06):** **JWT confirmed** as the token model (reuse Console auth + user-ID per call — see #9). This row now stays open only on the **public-sandbox key build** itself (not yet built). | Partner | Developer Support | [[developer-support]], [[sandbox-assist]] | 09-06 | Open |
| 32 | **Endpoint QA / test status** — ~80 endpoints, ~80–90% untested; affects mock-API fidelity for early build. When are they QA'd? **Update (18-06):** the YAML has been delivered; functional testing is underway (Postman + Claude automation) but **currently 0% passed** — some endpoints aren't working and testing is strict (fails if it doesn't work in Postman, because a client will hit it). Field/name changes are expected; the architecture is still open but the underlying functional behaviour won't change, so testing proceeds. Dev-portal / client view is tested first (published first). **Update (28-07):** latest spec is Friday's; the biggest issue now is **YAML quality** (missing limits etc.); the payloads themselves are relatively stable. A **program manager ID** field may be added for routing if the single-API-instance architecture holds (#48). Michael offered **UAT access + his API key** for earlier-stage development (checking with the team). **Update (29-07):** the Friday specs are now **in the vault** ([[txn-api-reference]]): the API is **split into external (client-facing) + internal (platform-management)** specs, with paths pluralised; these are the latest and the ones used going forward. **Update (03-08):** the pilot's **mock API is still built on the old May YAML** — *"it's currently on the old YAML file, but very easy to port it over"* — so the port to the July external/internal split is **outstanding work on the Novosapien side**, not a partner blocker. **Update (20-08): roughly 60% of the API is signed off.** Michael: *"pretty much I think we've 60% signed off of everything... it's more the transactional stuff from the latest stuff we haven't done yet."* Cardholder and card areas are solid; the transactional surface is the gap. He is also validating the likely endpoints Novosapien mapped per workflow against what TXN expects. **Correction (25-08):** the 03-08 note above is **stale and was wrong from 10 August**. The mock was regenerated against the External contract on 10-08 and now carries `api-specification_10Aug2026.yaml`: **67 paths, 98 operations, 327 schemas**, with a spec-pull pipeline so regeneration is routine. Evidence: `txn-mock-api` commits M1 and M2 on 10-08, deployed 11-08. The Novosapien-side port is **done**; what remains open is the original partner question, TXN's own endpoint QA pass rate. **Update (27-08): DT moved the spec URLs.** Michael confirmed DT **launched a new OpenAPI service** for better documentation and had to move the UAT specs to a new URL, with *"no further changes"* to content. New locations recorded at [[txn-api-reference]]. The move also changed the URL **shape**: the two specs now share a base path and differ by filename, where they previously differed by base path. That breaks an assumption in our own spec-pull script, tracked separately at #67. The partner question this row carries, TXN's endpoint QA pass rate, is unaffected. | Partner | Access Layer | [[agent-access-layer]] | 05-06 | Open, narrowed |
| 33 | **Console instrumentation depth** — page state, component identifiers, action handlers. Michael flagged additional work may be needed to "plug in the AI." **Update (18-06):** needs **Stackworkz to expose** page state / component identifiers / action handlers, plus likely a **caching mechanism** to hold session context; no caching requirements have been given to them yet (but they know the AI is coming). Discuss after the **design handover (9 July)**. This unlocks the **session-replay co-pilot** (Ian's "mum on the phone" example — know where the user started/went, then guide them back). | Partner | Co-pilot / Full Agentic | [[co-pilot]], [[full-agentic-experience]] | 13-05 | Open |
| 34 | **Umbraco doc APIs + vector index** — docs exposed via API for AI search, embedded for semantic retrieval; draft-API for the knowledge engine. **Update (18-06):** the **Umbraco API is available** for AI access/updates; a **UAT environment is up** (component mockups done, rendering docs from DT's API end-to-end). Moving to a **paid, cloud-hosted Umbraco** account (hosted by Umbraco, not self-hosted). The `LLMS.txt` file (#20) will live in Umbraco. | Partner | Developer Support | [[developer-support]], [[knowledge-engine]] | 09-06 / 10-06 | Open |
| 35 | **Console component library handle** — addressable React/MUI components for the agent to render (generative UI). **Update (18-06):** the handle is Super Ultra's **Figma handover pack** — component *frameworks* (buttons, page frameworks) that Stackworkz develop against. The agent **passes arguments** to pre-built components (rather than generating component code) → fully interactive (e.g. "show my card programs" → renders 5 card-program components, click into a modal). Mix of **shared** and **slightly-unique** components. Portal handover pack available now; **console pack at the 9-July handover**; Lovable prototype access possible. **Update (13-08):** **final wireframes now exist** and supersede the prototype; Michael has them and will share. Still to settle: **where the agent lives**, the **AI button in the header**, and how both translate into the **knowledge hub**. Rebuilding the UI against final designs is **two to three days**, since the functionality behind the agent is portable and only the surface changes. | Partner | Full Agentic Experience | [[generative-ui-rendering]] | 29-05 / 05-06 | Open |
| 36 | **A2A provider enablement** — direct A2A connections from the big providers (e.g. Anthropic). MCP-as-message fallback carries it until then. **Answered (18-06):** design confirmed — a **thin MCP (1–2 tools) on top of the main MCP** that **passes a text message through**: the external LLM (Claude / ChatGPT) sends a text message via MCP → the TXN agent → uses the main MCP server to do the work → result fed back. Adopt **direct A2A** from providers if/when it's widely available; few use it today. | Partner | Access Layer (A2A edge) | [[a2a-endpoint]] | 05-06 | Answered |
| 37 | **Process-surfacing altitude** — how much of the multi-agent work to show. "Won't know until tested in the wild" — land one universal level &amp; iterate. **Update (03-08):** now a live question against a working build. Tool calls currently stream in full, including granular internal steps George judged *"not relevant to show a user"*. Three options on the table: **show every tool call**, **show a summary the user can click into**, or **show only "I'm working" / "I've been doing this in the background"**. Michael's related steer: don't show too much detail, and drive users toward the canvas via descriptive prompts (e.g. "view transaction") rather than dumping detail in the chat. Undecided — to be settled by TXN using the deployed build. | Design | Co-pilot / Full Agentic | [[process-surfacing]], [[full-agentic-experience]] | 04-06 / 05-06 | Open |
| 38 | **Real-time vs cached writes** — console-wide principle: fire each call live, or cache &amp; commit on review? (Mike: depends on action type — confirm console-wide.) | Design | Co-pilot | [[co-pilot]] | 04-06 | Open |
| 39 | **Stuck-user replay mechanism** — restate goal → replay completed steps → correct. Agreed in principle, undesigned. **Update (18-06):** reaffirmed as a desired co-pilot feature; depends on Console instrumentation + caching (#33). | Design | Co-pilot | [[conversational-qa]] | 04-06 | Open |
| 40 | **Bounding open-ended composition** — the agent "starts with all the data but doesn't know where it's going" (Stackworkz). Guard rails on what it may compose — the hardest problem. | Design | Full Agentic Experience | [[generative-ui-rendering]], [[agent-orchestration]] | 05-06 / 29-05 | Open |
| 41 | **Generative UI inside the client's Claude** — "probably could, but not exactly the same way." How much renders through the A2A path vs the TXN surface? | Design | Full Agentic / A2A | [[full-agentic-experience]], [[a2a-endpoint]] | 05-06 | Open |
| 42 | **One MCP server vs several** — leaning to a single "monolith" with per-turn tool gating; confirm as the tool count grows. **Update (03-08):** v1 is built and the **deployment shape is settled even if the count is not** — the MCP server runs as a **separate service on top of the API rather than inside it**, because Novosapien's usual embedded pattern needs ownership of the deployed API and **DT owns the Core API**. Monolith-vs-several remains open on tool count. | Design | Access Layer | [[agent-access-layer]] | 01-06 | Open |
| 43 | **Simulation scope &amp; timing** — vision puts the synthetic-persona harness in Internal Ops scope; the map parks it as a launch component. When is it built / used (incl. pre-launch cold-start)? **Update (18-06):** reinforced by Novosapien's build method — **synthetic/simulated environments and mock APIs** (replicating endpoint structure) let component builds proceed in parallel ahead of real APIs/front-ends, with demo-data flags dropped as real data is stitched in. **Update (24-06):** testing methodology confirmed (aligned decision) — **headless AI-to-AI testing** in the admin-vault review space runs defined user journeys **thousands of times in parallel** to validate success criteria and catch prompt / tool-call / MCP-stability issues; runs **July→September**, replacing manual onboarding-flow testing. **Update (28-07):** pilot step one is the **synthetic-data environment**; TXN to review whether the data feels like live. The mock API **randomises responses** so demo runs never repeat identically. **Update (03-08):** the AI-testing workforce is **in use on the pilot** — George and Hasan test by hand, but *"a lot of these are AI testing itself… having a workforce that's going through testing, making sure everything's as needed."* Hardening against demo-time failures (a suspension call failed live) is the current focus. | Design | Internal Ops | [[internal-ops-agents]] | 10-06 | Open |
| 44 | **Dev-Support ↔ Internal-Ops boundary** — triage entry point (portal) vs resolution + knowledge loop (internal ops). Confirm the hand-off contract. | Design | Developer Support / Internal Ops | [[developer-support]], [[internal-ops-agents]] | 10-06 | Open |
| 45 | **Component success metrics** — several §7 sections are placeholder-quality; quantify the evals per component with TXN. | Design | Cross-cutting | [[components]] | (multiple) | Open |
| 46 | **Internal Ops vision / dedicated session** — the component is assembled from the vision + adjacent sessions + journeys; Ian said it needs its own vision (resolves #25, #43, #44). **Update (18-06):** Brett's plan — Internal Ops will be **populated incrementally** from standups and meetings as ideas land, rather than one big session (Reconciliation #25, by contrast, gets its own focused session). | Design | Internal Ops | [[internal-ops-agents]] | 10-06 | Open |
| 47 | **Self-healing *code* ownership** — can agentic self-healing extend to *code* (Sentry → investigate → open a PR), or only docs/knowledge? **Answered (11-06):** self-healing *code* is **off the table for now** — DT owns the codebase post-handover with strict framework/library policies and won't allow it; Novosapien also lacks log/data access yet (DT firewall blockers). Focus is self-healing **documentation/knowledge** (the [[knowledge-engine]]). Revisit if architecture/access opens up. **Update (18-06):** DT-codebase position unchanged, **but Novosapien-side self-healing is in scope** for its own MCP/agents (e.g. a 422 on a changed endpoint → Slack alert → diagnose → fix the YAML/docs in minutes). Future **crossover** when/if TXN takes over development (Novosapien raises issues → DT fixes, expanding over time). | Partner | Internal Ops | [[self-healing-docs]], [[internal-ops-agents]] | 11-06 | Parked |
| 48 | **DT multi-tenancy model** — DT built **one central system for every client**; Novosapien/TXN designed it **per-client**. The API gateway is a single containerised instance, but databases are per-client. Both work technically; the driver is **commercial** (clients expect their data fully separate) and **risk** (per-client limits one client's load/blast-radius affecting others). TXN is **leaning per-client** and wants to be able to defend that position; being ironed out with DT. **Update (24-06):** now a **live disagreement** — Ian (TXN CEO) wants **individual ring-fenced client stacks** for security; DT proposes **central API management + an orchestration layer**. To be resolved with the CTO (Michael) — the *Confirm Architecture* action. Highest-leverage open item; it changes the deployment shape downstream. **Update (28-07):** the DT architecture discussion is **down to four points** but still unresolved; Dorte flagged it as the foundation everything else comes right from once settled. **Update (18-08): DT is confirmed as the binding constraint on the wider timeline.** Brett offered to finish the pilot a couple of weeks early; Dorte said it would help *"tremendously"* but changes nothing, because **DT has to finish their build first** and *"everyone else is finishing before they finish"*. Novosapien delivering earlier does not pull the launch in. **Update (20-08): the question has changed shape.** It is **no longer a cost or tenancy conversation**. Michael: *"it's not really a technical thing anymore... it's an IP discussion."* DT wants to **retain its IP and its access**; TXN needs DT to have **no access**, because under GDPR *"we can't have people in South Africa looking at our data"*. This holds whether the platform sits on DT's tenant or TXN's, and it is now **at board level**. That moves it out of the architecture team's hands and makes the resolution date less predictable. | Partner | Architecture / Access Layer | [[integrations]], [[architecture]] | 18-06 | Open |
| 49 | **TXN ↔ DT infrastructure separation** — DT's current build sits **inside the DT domain** (their email addresses, VPNs) and uses **3+ core services shared with DT's own clients**, which won't work for TXN (DT can grant access to TXN, not the reverse). End goal: **TXN entirely separate from DT**, hosted on **TXN-controlled Azure in Europe** (moving from the South-Africa UAT). Novosapien is containerised so can build/test on any cloud until production. Mike is pushing DT heavily; needs the environment stood up before access can be granted. **Update (24-06):** Novosapien's AI architecture is **containerised** (Azure **or** GCP; Terraform + Azure DevOps CI/CD), so infra *location* is less critical for portability. Remaining gap: the **AI-specific components inside Azure aren't yet specified** (Ian's operationalisation concern) → the *Define AI Requirements* action (Brett + George). **Update (28-07):** Novosapien is ready to **terraform and deploy everything as soon as the architecture is decided**; quiet on infra from the Novosapien side until then. | Partner | Architecture | [[integrations]], [[workstream-1-2-architecture]] | 18-06 | Open |

| 50 | **TXN reviewer availability across the acceptance window** — **Michael is on holiday early September for 10 days** (raised by Dorte, 13-08). The pilot completes **7 September**, so the absence lands on the **formal UAT fortnight and the acceptance review**, and Michael is TXN's only technical reviewer. Dorte flagged it precisely so the work can be planned around it and he still gets a proper break. Needs a decision: bring the review forward, nominate a second reviewer, or move the acceptance date. | Client | Delivery / Acceptance | [[delivery]] | 13-08 | Open |
| 51 | **TXN has not yet used the review build** — neither Michael nor Dorte has been into the deployed environment. Dorte: *"Honestly, I really like it, but I haven't had the chance to get onto it. And Mike is the same... that's the biggest concern I have at the moment."* Michael is carrying user stories, UAT testing, TXN's own AI build and company-wide readiness. This matters because **three open items are designed to be settled by TXN driving the build**: the irreversible-action policy (#26), tool-call visibility (#37), and the field-level detail per persona. Without usage they stay open by default. **Update (18-08): partly resolved.** **Michael delivered written feedback on Friday 14 August** and most of it is already implemented, so the technical reviewer is now engaged. **Dorte still has not been into the build** and will look next week after the 18 August deployment; she was explicit that Michael is the one who matters, *"his word as gospel"*. The residual risk is therefore concentrated on a single reviewer, which is what #50 is about. **Update (20-08): the cause is now known and it is fixable.** **Ian cannot get in**: he is hitting a **404 on the login page** and has never logged in. George picked it up on the call to fix immediately. Michael's plan is for **Ian and Dorte to run the non-technical UAT** (look and feel, the things a technical reviewer overlooks) while he covers the technical side. Michael has meanwhile **paused his own UAT** to get ahead on user stories, which are being reworked mid-build. | Client | Delivery | [[delivery]], [[full-agentic-experience]] | 13-08 | Open |
| 52 | **One brand palette across all surfaces** — Dorte (13-08) prefers the **knowledge hub's green and blue** over the pink, notes the marketing **website is much louder** than the Control Center and knowledge hub, and wants a single palette used consistently. Decision sits with Ian on his return, with Bronwyn owning the website. Relevant to the corporate identity pack, where the accent colour is already recorded as contested. | Client | Brand / Cross-cutting | [[commercial]] | 13-08 | Open |

| 53 | **Security and user access deferred within the pilot** — confirmed 18-08 as a deliberate choice: features first, on the reasoning that **no real APIs are touched** in the pilot. One or two of Michael's 14 August feedback items were security-related. The stated order puts **MCP hardening, user access and security after the workflow work**, so they land in the final stretch of the six weeks. Acceptable while everything runs on the mock; it becomes the gating item the moment a real API is in scope, and it compounds with the deferred permission model (#2, #9). **Update (20-08): a structural argument was given for why the deferral is safe for now.** Because **the MCP tools sit outside the agent** rather than within it, the worst case is generated code calling an MCP tool, and that tool already carries its own validation and limits. Most of what appears exposed is read-only. **Sub-agents are the piece that will need hardening**, and they are not yet wired. | Design | Access Layer / Delivery | [[mcp-server]], [[permission-scoping]] | 18-08 | Open |
| 54 | **Novosapien team away from 10 September for three weeks** (Bali working retreat; flying the evening of the 10th, travelling the 11th, working from the 14th). Standups move to 09:00 UK. **The pilot completes 7 September**, so acceptance, the 5 business day shortfall window and any remedial work all have to land in the three days before the team travels. Combined with #50 (Michael away 10 days in early September) this is the tightest point in the engagement calendar. Needs the acceptance date and its contingency agreed explicitly, not assumed. | Client | Delivery / Acceptance | [[delivery]] | 18-08 | Open |

| 55 | **Does TXN hold balances, or not?** The vault states in five places ([[vision]] lines 39, 43, 45, 122, 355) that in MVP TXN does not hold funds and **every authorisation is a pass-through** to the client's system of record. On 24-08 the onboarding skill put that discrepancy to Ian live and he corrected it: *"there are occasions where we can authorize a transaction based on a balance that we hold on behalf of the client... increasingly in this market it typically is that the authorization gets rooted to the customer for them to make a decision. But it's an either or. Sorry, it's it's both, not an either or."* This is **load bearing**: the pass-through model is the stated reason [[fraud-risk-assist]] is scoped as *advise, don't decide*. Needs Ian to confirm the position, then [[vision]] corrected and the Fraud & Risk Assist scope re-checked. | Client | Vision / Fraud & Risk | [[vision]] | 24-08 | Open |
| 56 | **Multi-tenancy: one client can still affect the others.** Ian (24-08) gave the full history: the original plan was **one environment per client**; DT came back with a **centralised resource** managing everyone into one place with APIs hitting segregated databases; clients can still stand up a completely separate environment if it matters to them. Asked whether a client flooding the system with bad API calls could impact others: *"Yes, we believe there still is a risk that it could impact other clients."* Described as an **ongoing conversation between Ian and DT** about the proposed architecture. Not currently recorded in [[architecture]]. Clients will need assurance on both data separation and system stability. | Partner | Architecture | [[architecture]] | 24-08 | Open |
| 57 | **One engineer is committing to every TXN build repository.** Evidence (25-08): every commit on `txn-console-react`, `txn-agentic-agent`, `txn-mock-api` and `txn-mcp-server` since kickoff on 27-07 is George's. Thirteen days from acceptance that is a bus factor of one. Compounding it, the 24-08 outbound session assigned George **email configuration and domain warming** for the Outbound Workforce plus a session with Jacob, TXN's domain administrator, which is a **different engagement drawing on the only person building the pilot inside the pilot's launch window**. Related: build velocity across those four repos has fallen to **two substantive commits in seven days**. Needs an explicit decision on which gives way if the two collide. **Update (27-08): the velocity half is withdrawn and was a measurement error.** The count was taken on `origin/main`; the work was on `build/three-production-workflows`, `build/workflow-slate` and `build/agentic-experience` and merged on 26-08, roughly 5,000 lines across the four repositories ([[2026-08-26-workflow-slate-build]]). Method corrected: count all branches. **The single-committer half stands and is unchanged** — every commit in the 26-08 merge is also George's, and the 26-08 Stackworkz code-sharing session adds a further call on the same person. | Internal | Delivery | [[delivery]] | 25-08 | Open, narrowed |
| 58 | **The workflow slate has no cut line.** TXN's 25-08 decision ([[2026-08-25-workflow-slate-decision]]) ranked all ten candidates and **parked or rejected none**, where the slate had asked for build next, park, or not this. A rank order without a line under it does not bound scope. Ranks 8 and 9 (create a monitoring alert; scheduled performance report) are **partner blocked regardless of position**, the first needing Direct Transact to agree an alert write and the second needing the data lake, so they cannot be scheduled on rank alone. Needs TXN to say where the line falls. | Client | Delivery / Scope | [[delivery]] | 25-08 | Open |
| 59 | **Where does the ranked workflow build land, and is it understood as post-pilot?** Michael's 25-08 email frames the ranked order as *"the initial workflow build in the pilot"*. The pilot build definition is frozen and completes **7 September**, so none of this can enter it. Both sides should hold the same picture before the acceptance review rather than after it. Related: TXN will confirm endpoints and approval gates *"through next week"*, which is input arriving in the pilot's final fortnight ([[open-questions]] #57 on capacity). **Update (27-08): the question is now urgent rather than tidy, because the code exists.** On 26-08 the slate build merged SOPs for **ranks 1 to 6** with full tool coverage ([[2026-08-26-workflow-slate-build]]). Five of them are, by this row's own reasoning, post-pilot scope that has been built inside the frozen window. Three consequences. **One:** they are counted as delivered work but **not** as pilot completion, so the percentage on the flight plan does not move for them. **Two:** they were written **before** TXN confirmed endpoints and approval gates, so where TXN's thirteen journey documents disagree with the implemented gates, the implementation moves. **Three:** if these are inside the pilot, the acceptance bar has silently widened from six workflows to thirteen, which no one has agreed. Needs Michael to say which. | Client | Delivery / Acceptance | [[delivery]] | 25-08 | Open, now urgent |
| 60 | **Who is the operator?** Long-carried question: is the pilot's operator a **customer-service agent** or a **programme administrator**? Michael's 25-08 wording points to the former, *"where our operators naturally start"* and *"explained in language we can give a customer"*, but it has not been stated explicitly. It shapes **field-level detail per persona** (one of the three items still needing TXN inside the build) and the design of several ranked workflows, notably card service actions and cardholder offboarding. **Update (27-08):** both of those workflows now exist as SOPs on `main`, written against the customer-service reading of the operator. `card-service-actions` is explicitly framed as *"the daily single-target card service actions, one approval card each and low ceremony"*, which is a service-desk shape, not an administrator's. The assumption is now embedded in code rather than held in a document, so confirming it has become cheaper to do now than to unpick later. | Client | Full Agentic Experience | [[full-agentic-experience]] | 25-08 | Open, strong signal |
| 61 | **Which workflows are in the pilot was never confirmed** *(re-filed from #55 on 25-08 when the branch merge found two rows sharing that number; superseded by [[2026-08-25-workflow-slate-decision]], with the residual gap carried at #58)* — the three currently operational (spend-control change, card suspend and reactivate, plus the read-only SOPs) were chosen by Novosapien to prove the pattern, not selected by TXN. The candidate slate was sent for a decision on every card (build next, park, not this) and **no confirmation has come back**; on 13 August Michael said he would review it against TXN's own vault, and on 18 August George repeated the ask: *"if there's some that you think, right, we definitely want those ones, let us know and we'll start working on them."* **Consequence: the pilot's workflow scope is open, so completeness cannot be judged against it.** Novosapien is close to complete on everything it can do without the answer; further workflows are ready to start the moment TXN picks. This is the single decision gating the pilot's finish line. **Update (19-08): Novosapien has narrowed the question from ten candidates to seven.** Three are parked on our own recommendation: **#9 scheduled performance report** (not proceeding, substantial extra back-end work plus the undelivered data lake), **#7 monitoring alert by conversation** (not proceeding, needs DT to agree and build an alert write), and **#10 guided product launch** (undecided, leaning out: the largest build on the slate and by TXN's own framing the least frequent operation). Parked rather than dropped, and shown as such in the flight plan, since all three were Novosapien's own suggestions. **Update (20-08): resolving.** Michael has reviewed the slate: *"I went through it last week. I'm fairly happy with what you've sent across... they're very much aligned to what we want to do."* He met Ian straight after the standup to agree the **priority order** and will send the list back. **Target agreed: six or seven workflows for the pilot**, with a floor of three; additions need not come from the slate, since any TXN user journey can be substituted. TXN also independently confirmed the three net-new candidates sit outside the pilot. | Client | Full Agentic Experience / Delivery | [[delivery]], [[full-agentic-experience]] | 18-08 | Closed, superseded |
| 62 | **How does the agentic surface reach the Stackworkz Console?** A 26-08 session demonstrated the agent interface to Stackworkz and opened a plan for sharing code between the two build teams ([[2026-08-26-stackworkz-agent-demo]]). The pilot runs on `txn-console-react`, a full React build Novosapien produced so the experiment had a real surface to drive; **Stackworkz build the production Console and the Developer Portal** in C#/.NET with React and Material UI on their own infrastructure. Every wire-in plan to date has treated the hand-off as a Direct Transact problem; the Stackworkz half has had no named mechanism until now. Undecided: whether the surface is **handed over as code**, **consumed as a service** by the Console, or **kept separate and linked to**; which repository holds shared code and who merges to it; how a push-to-`main` deploy cadence reconciles with a DevOps pipeline onto VMs; and whether any of it lands before or after acceptance on 7 September. This is the first mechanism that makes **#33** (Console instrumentation depth) and **#35** (component library handle) actionable rather than theoretical. No transcript was captured, so get the follow-up session recorded. | Partner | Architecture / Full Agentic Experience | [[integrations]], [[full-agentic-experience]] | 26-08 | Open |
| 63 | **When are Bronwyn's brand pack and the LinkedIn logins loaded?** Ian confirmed on 21-08 that he has sent Bronwyn's tone-of-voice PowerPoint and asked that it be *"fed in in advance of those interviews"*, which makes it a **gating input on the brand entity session**, not a general owed item. The LinkedIn logins remain outstanding and gate channel connection. Both sit on the Content Workforce critical path: **TXN gets no platform access at all until the manifesto, the pillars and the entities are uploaded** (Max, 21-08: *"once all three are uploaded, then it's all yours"*), and the manifesto is the only one loaded. Related to **#52**, the single brand palette, since Brett has offered to turn the pack into a proper brand CI. | Client | Content Workforce / Brand | [[content-workforce]], [[content-entities]] | 21-08 | Open |
| 64 | **Seven working days have to carry three engagements and an acceptance.** The 27-08 GTM reschedule concentrates everything into **Tue 1 to Wed 9 September**: the content pillars and brand entity (1 Sep), **six personal content interviews, two each for Ian, Michael and Dorte** (captures Tue 1 to Thu 3 Sep, refinements Tue 8 to Thu 10 Sep), the Outbound email-domain and warming session with Jacob (1 or 2 Sep), the Outbound team login and setup session, the pilot regression pass over thirteen SOPs, formal UAT, and **pilot acceptance on 7 September**. Two hard walls bound it: **Michael is away ten days from early September** (#50) and is needed for both the acceptance review and his own entity session, and the **Novosapien team flies on the evening of 10 September** for three weeks (#54). Nothing here can be recovered by moving it right, because there is no right to move into. **Update (27-08): the personal sessions doubled from two blocks to six interviews**, two per person, in the same fixed window. Monday 31 August is the UK summer bank holiday and the team flies the evening of Thursday 10 September, so the window is eight working days and cannot be extended at either end. **Two specific decisions are needed.** First, Michael needs **two** sessions, on 1 and 8 September, and his leave covers most of that; his 1 September capture is the one to protect, and his 8 September refinement slipping to October is the workable fallback to agree now. Second, who absorbs the collision if the domain session and the pilot's must-land list both need George in the same week (#57). | Internal | Delivery / GTM | [[delivery]], [[content-workforce]], [[delivery-schedule]] | 27-08 | Open |

| 65 | **No boundary between what the workforce answers and what always goes to a human.** Ian, 27-08, when asked how an agent should handle a buyer digging into the platform's origins: *"we've got to be careful about the type of automation that we're comfortable handling, the type of queries we're comfortable handling with automation, and those that will never be handled by it."* His own move on that specific question would be to answer *it's a TXN platform* and then work out what sits behind the question, because the concern might be technical debt and you cannot know without asking. **That is not scriptable, and no such boundary is defined today.** It is a workforce configuration decision rather than an offer question, and it needs its own conversation: which query classes the Cold Outreach Workforce answers, which it hands off, and what the hand-off looks like from the prospect's side. Raised in [[2026-08-27-outbound-workforce-offer-session-2]]. | Internal | Outbound Workforce | [[outbound]] | 27-08 | Open |
| 66 | **JV Messaging Framework v2.2 asserts a negative that Ian says is not literally true.** v2.2 §1 states the DT track record is TXN's *"not because TXN is built on top of DT's platform"* and *"not because TXN was built on DT's system"*, and its Test 1 requires any sentence implying otherwise to be rewritten. On 27-08 Ian said elements did start from the DT code base, and that the correct handling is simply **not to discuss lineage in either direction** ([[2026-08-27-outbound-workforce-offer-session-2]]). Staying silent and asserting the negative are different positions, and an agent working from v2.2 would assert the second. **Ian's own view is that this is overthought** and that nobody is likely to press on it, so this is filed as a documentation-hygiene item rather than a live risk. **Update (27-08): v2.3 arrived and does not close it.** v2.3 changes one thing only, the Section 7.4 'do not volunteer' redline; §1 and Test 1 are word for word identical to v2.2, so both assertions still stand in the canonical document. The fix, if wanted, is to soften those two sentences from asserting the negative to simply not raising lineage. | Client | Outbound Workforce / Messaging | [[outbound]], [[source-documents]] | 27-08 | Open, low |
| 67 | **Our spec-pull script cannot fetch the internal spec from DT's new URL.** `txn-mock-api/scripts/fetch_spec.py` holds a single constant, `SPEC_ENDPOINT = "api-specification.yml"`, and builds every request as `{base_url}/{SPEC_ENDPOINT}`, switching only the base URL on the `--internal` flag. That was correct while DT served the two specs at **different base paths under the same filename**. From 27-08 they sit at the **same base path and differ by filename** (`api-specification.yml` and `internal-api-specification.yml`), so pointing both base URLs at the new path makes `--internal` fetch the **external** spec. **It fails silently**: the request succeeds, the YAML parses, and the file is written under the name that was asked for, so a wrong-but-valid spec would be accepted without complaint. **Not currently biting**, because the pilot builds from the committed `spec/api-specification_10Aug2026.yaml` pinned by sha and nothing on the critical path does a live fetch; it bites the next time anyone regenerates from the internal spec. Fix is one line, making the filename depend on the flag. Ours to fix, not DT's. | Build | Access Layer | [[txn-api-reference]], [[agent-access-layer]] | 27-08 | Open |

## Notes

- New open questions raised in any extraction go here (with a backlink), not at the bottom of a single doc.
- When a question is answered, set its status to `Answered` and record where the answer now lives.
- The HTML gap register ([drafts/txn-gap-register.html](drafts/txn-gap-register.html)) is a generated view of this table — keep it in sync when rows change. **Pending regeneration after the 18-06 update.**
